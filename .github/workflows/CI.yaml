name: CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: paratools/salt-dev:latest
    steps:
      - uses: actions/checkout@v3
      - name: Print working directory
        run: |
            echo "Working directory: $(pwd)"
            echo "Working directory contents:"
            ls -la
